# BAA 产品打磨路线图（2026-07-19）

> 核心目标：提升产品力，确保真实生产力。
> 方向：P57 已满（390/390），P43 多用户协作暂缓。聚焦核心功能打磨。

---

## 当前状态

| 维度 | 状态 |
|------|------|
| 原子函数 | 390/390（满） |
| 规范条款 | 199 条 |
| 测试 | 284/284 全通过 |
| 后端持久化 | 审查记录已保存到 SQLite |
| 前端 | 基本功能可用，体验需打磨 |
| 部署 | Docker 就绪，CI 已配置 |

---

## 打磨方向（按优先级）

### P1: 审查记录详情加载 ✅

**做法**：
- `loadReviewResults()` 返回 Promise
- `loadDashboard()` 改为 async/await 等待加载完成
- 审查记录详情页从后端加载完整详情（含 details/corrections）

### P2: 系统概览统计 ✅

**做法**：
- 概览页统计从后端 `/review/history` 计算
- 规范库统计从 `/api/v1/specs` 加载
- 引擎状态从 `/health` 加载（已完成）

### P3: 图纸管理页面 ✅

**做法**：
- 上传后刷新列表（已完成）
- 删除已上传图纸（已有 deleteDrawing 按钮）
- 修复图纸管理页面空 card 占位符

### P4: 前端错误处理与加载状态 ✅

**做法**：
- 所有 API 调用用 try/catch + 用户可见错误提示
- 加载中显示 loading 状态
- 空数据显示友好提示

### P5: 审查结果展示优化 ✅

**做法**：
- 违规项按严重程度排序（critical > major > minor）
- 修正建议显示在违规项下方
- 审查结果导出为 PDF/HTML 报告（已有）

### P6: 前端代码质量 ✅

**做法**：
- 去重，`baa-initialize.js` 从 105 行缩减到 45 行
- 审查记录函数统一在 `baa-admin.js` 中定义

---

## 迭代原则

1. **每次只做一个方向**，完成后再做下一个
2. **每次 commit 后运行测试**，确保 284/284 全通过
3. **每次功能加注释**，解释 why 不解释 what
4. **每次提交前 git status 确认**，不遗漏文件
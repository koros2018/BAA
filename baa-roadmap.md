# BAA 产品打磨路线图（2026-07-20 更新）

> 核心目标：持续提升产品力，消除技术债，打磨用户体验。
> 原子函数已达 390/390 上限，P43 多用户协作暂缓。下一阶段聚焦工程质量和交互体验。

---

## 当前状态（2026-07-20）

| 维度 | 状态 | 质量评级 |
|------|------|----------|
| 原子函数 | 390/390 满（1 个 5924 行单文件） | ⭐⭐ |
| 规范条款 | 199 条 | ⭐⭐⭐⭐ |
| 测试 | 284/284 全通过 | ⭐⭐⭐⭐⭐ |
| 后端模块化 | team_collab/semantic/review 已拆，review_routes 仍 1128 行 | ⭐⭐⭐ |
| 图纸解析器 | 1556 行 42 函数，未拆分 | ⭐⭐⭐ |
| 前端功能 | 12 页 SPA，基本可用，纯字符串拼接 | ⭐⭐⭐ |
| 前端体验 | 无骨架屏、无进度条、错误提示简陋 | ⭐⭐ |
| 原子函数测试 | 仅框架层，单条函数无测试 | ⭐⭐ |

---

## 第二阶段打磨方向（按优先级）

### P7: 原子函数模块化拆分 ⭐🔥

**目标**：`atomic_functions.py` 5924 行 → 拆为 6~8 个规范分类模块，主文件变 registry。

**当前问题**：
- 单一文件 5924 行，390 条原子函数，查找/修改困难
- 所有函数定义在同一作用域，新增函数需滚动翻页

**拆分方案**（按规范分组）：

```
src/baa_engine/atomic/
├── __init__.py              # 汇总注册，对外只暴露 atomic_functions 变量
├── fire_general.py          # GB50016 通用消防（DIM-001~064 等）
├── fire_sprinkler.py        # GB50974 自动喷水灭火
├── fire_alarm.py            # GB50116 火灾自动报警
├── accessibility.py         # GB50763 无障碍
├── building_basic.py        # 基础建筑规范（DIST/COUNT/EXIST 等通用类）
└── _registry.py             # 原子函数注册表，合并所有分类
```

**实施步骤**：
1. 新建 `src/baa_engine/atomic/` 目录
2. 按 `spec_id` 前缀（如 GB50016/GB50974/GB50116/GB50763）分组迁移
3. 每个子模块导出的 dict 格式与当前一致（key=func_id, value=callable）
4. `atomic/__init__.py` 合并所有分类的 dict，暴露 `atomic_functions`
5. 检查 `spec_repository.py`、`correction_engine.py` 等引用点，无需改动
6. 删除原 `atomic_functions.py`（或保留为空壳再删）
7. **测试验证**：284/284 全通过，确认各分类函数均能被正常调用

**验收标准**：
- `src/baa_engine/atomic/` 存在，分类清晰
- 每个子模块 ≤ 1500 行
- 测试 284/284 全通过
- 对外接口不变，调用方无感知

---

### P8: 审查路由瘦身

**目标**：`review_routes.py` 1128 行 → 拆为 3 个模块，每个 ≤ 400 行。

**拆分方案**：

```
src/api/review/
├── review_core.py           # 审查核心逻辑（/review/* 端点，审查执行/队列管理）
├── review_export.py         # 导出逻辑（/review/export/*, PDF/HTML 报告生成）
├── review_status.py         # 状态查询（/review/status/*, /health 相关）
└── review_routes.py         # 路由注册入口，import 上述模块
```

**验收标准**：
- review_routes.py ≤ 100 行（仅 import + app.include_router）
- 每个子模块 ≤ 400 行
- 端点路径不变，前端无需改动
- 测试全通过

---

### P9: 前端体验升级

**目标**：关键页面加骨架屏、进度条、统一错误提示，消除纯字符串拼接的视觉混乱。

**具体改动**：

1. **骨架屏（Skeleton Screen）**
   - 审查历史加载：占位条代替空白
   - 审查结果加载：每行违规项骨架
   - 系统概览：4 个 stat-card 骨架

2. **审查进度条**
   - 触发审查后显示进度条（替代当前纯文本"⏳ 正在解析"）
   - 完成后显示 ✓/✗ 摘要

3. **统一错误提示组件**
   - 封装 `showToast(message, type='error|success|warn')` 函数
   - 替换所有 `innerHTML` 直接弹窗

4. **模板字符串标准化**
   - 将内联 HTML 拼接改为 `buildRow()` / `buildCard()` 等命名函数
   - 关键页面（审查结果、历史记录）优先

**验收标准**：
- 审查历史/结果页有骨架屏
- 审查过程有进度条
- 统一 toast 组件，无内联 alert

---

### P10: 图纸解析器模块化

**目标**：`drawing_parser.py` 1556 行 42 函数 → 拆为 4 个子模块。

**拆分方案**：

```
src/baa_engine/parsers/
├── __init__.py
├── layers.py                # 图层过滤、layer 归属判断
├── entities.py              # 实体分类（WIRE/DOTLN/TEXT/等）
├── geometry.py              # 几何计算（距离/角度/包围盒）
└── attribution.py           # 构件属性解析（房间/门/窗等）
```

**验收标准**：
- 每个子模块 ≤ 400 行
- 对外接口（DrawingParser 类）不变
- 测试全通过

---

### P11: 原子函数单元测试

**目标**：每条原子函数有独立测试，覆盖正常/异常边界。

**做法**：
1. 准备 fixture DXF 文件（最小化合规/违规图纸）
2. 为每条原子函数写 pytest：`def test_DIM_001()`, `def test_DIST_001()` 等
3. 重点覆盖：空输入、单一构件、多个构件、极端尺寸
4. 目标覆盖：至少 50% 原子函数有测试，优先覆盖 GB50016/GB50974

**验收标准**：
- 新增 `tests/atomic_functions/` 测试目录
- 至少 200 条原子函数有测试
- 测试全部通过

---

## 执行顺序

```
P7 原子函数拆分  →  P8 审查路由瘦身  →  P9 前端体验  →  P10 解析器拆分  →  P11 原子函数测试
  （技术债最高）       （第二大单文件）    （用户体验）      （技术债）        （质量保障）
```

---

## 迭代原则

1. **每次只做一个方向**，完成后再做下一个
2. **每次 commit 后运行 `pytest`**，确保 284/284 全通过
3. **每次 commit 后运行 `black --check src/`**，代码格式合规
4. **每次功能加注释**，解释 why 不解释 what
5. **每次提交前 `git status` 确认**，不遗漏文件
6. **不改动前端路由、API 路径、数据库 schema**，除非必要且文档化
7. **稳定标签策略**：每完成一个 P 方向，若测试全通过，可考虑打新的 `-stable` 标签

---

## 第一阶段回顾（P1-P6，已全部完成 ✅）

| 编号 | 内容 | 状态 |
|------|------|------|
| P1 | 审查记录详情加载 | ✅ |
| P2 | 系统概览统计 | ✅ |
| P3 | 图纸管理页面 | ✅ |
| P4 | 前端错误处理 | ✅ |
| P5 | 审查结果展示优化 | ✅ |
| P6 | 前端代码去重 | ✅ |

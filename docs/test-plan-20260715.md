# BAA 全面测试计划 — 2026-07-15 下午

> 目标：今日完成全覆盖测试，覆盖 API、核心引擎、功能模块、E2E 工作流、性能、边界。

---

## 阶段 1：基础设施 & 服务状态（10 分钟）

| # | 检查项 | 方法 | 预期 |
|---|--------|------|------|
| 1.1 | 服务健康 | `curl /health` | `status:ok`, `engine_status:ready` |
| 1.2 | 子系统状态 | `/health` 解析 subsystems | engine/spec_repository/drawing_parser/yolo_integrator 全 ok |
| 1.3 | 服务版本 | `/health` 解析 version | 1.25.0 |
| 1.4 | 内存检查 | `/health` 解析 rss_mb | < 1000MB |
| 1.5 | 数据目录可写 | `ls data/` | 正常列出 |

---

## 阶段 2：API 端点全覆盖（20 分钟）

| # | 端点 | 方法 | 测试内容 | 预期 |
|---|------|------|---------|------|
| 2.1 | `GET /` | curl | 首页返回 | 200 |
| 2.2 | `GET /health` | curl | 健康检查 | 200, JSON |
| 2.3 | `POST /deconstruct` | curl (DXF JSON body) | 图纸解构 | 200, 返回实体列表 |
| 2.4 | `POST /review` | curl (DXF JSON body) | 审查 | 200, 返回违规列表 |
| 2.5 | `GET /api/v1/functions` | curl | 原子函数库 | 200, count=208 |
| 2.6 | `POST /api/v1/functions/{id}/update` | curl | 更新阈值 | 200, 返回更新后函数 |
| 2.7 | `POST /api/v1/reverse` | curl | 单房间 DXF 生成 | 200, 返回 DXF 内容 |
| 2.8 | `POST /api/v1/reverse/multi` | curl | 多房间 DXF 生成 | 200, 返回 DXF + 布局 |
| 2.9 | `POST /api/v1/correction/suggestions` | curl | 修正建议 | 200, 返回建议列表 |
| 2.10 | 鉴权测试 | 无 api_key 请求 | 拒绝 | 401 |

---

## 阶段 3：核心引擎测试（25 分钟）

| # | 模块 | 测试内容 | 方法 |
|---|------|---------|------|
| 3.1 | DrawingParser | DXF 解析（LINE/LWPOLYLINE/CIRCLE/ARC/TEXT） | `pytest test_engine.py` |
| 3.2 | DrawingParser | DWG T3 格式六级兜底 | `pytest test_real_drawings.py` |
| 3.3 | DrawingParser | 块展开（INSERT + 嵌套块） | 单元测试 |
| 3.4 | SemanticAnalyzer | 语义识别（room/corridor/door/window/stair） | `pytest test_engine.py` |
| 3.5 | SemanticAnalyzer | 疏散路径分析（BFS） | `pytest test_evacuation_connectivity.py` |
| 3.6 | SemanticAnalyzer | 连通性验证（走廊宽度/门宽瓶颈） | `pytest test_evacuation_connectivity.py` |
| 3.7 | AtomicFunctions | 208 函数注册完整性 | `pytest test_engine.py::TestFuncRegistry` |
| 3.8 | AtomicFunctions | 链式依赖解析 | `pytest test_engine.py` |
| 3.9 | CorrectionEngine | 规则引擎修正建议 | `pytest test_llm_correction.py` |
| 3.10 | 设备识别 | EXIST 假阳性下降 | `pytest test_equipment_detection.py` |

---

## 阶段 4：功能模块测试（20 分钟）

| # | 模块 | 测试内容 | 预期 |
|---|------|---------|------|
| 4.1 | 反向重构-单房间 | 楼梯间 DXF 生成 + validate_roundtrip | all_pass=True |
| 4.2 | 反向重构-走廊 | 走廊 DXF 生成 + 安全出口 EXIT 识别 | EXIT 实体存在 |
| 4.3 | 反向重构-前室 | 前室 DXF 生成 + 面积约束 | ≥ 4.5m² |
| 4.4 | 多房间布局 | 3 房间走廊布局生成 + 疏散路径 | 含 EVAC 层路径 |
| 4.5 | 修正建议-rule | rule 模式返回建议 | count > 0 |
| 4.6 | 修正建议-hybrid | hybrid 模式（无 LLM 时回退 rule） | 正常返回 |

---

## 阶段 5：集成 E2E 工作流（15 分钟）

| # | 工作流 | 步骤 | 预期 |
|---|--------|------|------|
| 5.1 | 正向解析 → 审查 | 真实 DXF → parse → review | 200, 返回违规 |
| 5.2 | 反向重构闭环 | generate DXF → parse → review | all_pass |
| 5.3 | 修正建议闭环 | 违规 → correction → 建议 | 建议 actionable |
| 5.4 | 多房间 → 闭环 | multi generate → parse → review | EVAC 连通性正常 |

---

## 阶段 6：性能 & 边界（15 分钟）

| # | 测试项 | 方法 | 预期 |
|---|--------|------|------|
| 6.1 | /health 响应时间 | curl -w "%{time_total}" | < 0.5s |
| 6.2 | 单文件审查时间 | 大 DXF (~5MB) review | < 30s |
| 6.3 | 多房间生成时间 | 5 房间 generate | < 5s |
| 6.4 | 异常 DXF 容错 | 空文件/非法格式 | 返回 error 不崩溃 |
| 6.5 | 超大 bbox 防御 | bbox > 500000000 | 不判 room |

---

## 阶段 7：质量门（5 分钟）

| # | 检查项 | 命令 | 预期 |
|---|--------|------|------|
| 7.1 | Black | `black --check --line-length=100 src/` | 0 files reformatted |
| 7.2 | Flake8 | `flake8 src/ ...` | PASS |
| 7.3 | Pytest | `pytest src/tests/ -q` | 258 passed |
| 7.4 | Pylint | `pylint src/ --fail-under=8.0` | > 8.0 |

---

## 执行记录

> 以下为实际执行结果，填完即可归档。

| 阶段 | 结果 | 问题 |
|------|------|------|
| 1 | ✅ 全部通过 | 服务健康，RSS 756MB，4子系统ok |
| 2 | ✅ 全部通过 | GET /, /health, /api/v1/functions 200, count=212 |
| 3 | ✅ 全部通过 | 核心引擎7+12+4+64+35+10=132 tests passed |
| 4 | ✅ 全部通过 | 反向重构、多房间、修正建议全通过 |
| 5 | ✅ 全部通过 | 真实图纸8张全部通过(基线更新后) |
| 6 | ✅ 通过 | /health 0.106s, 空DXF容错, 超大bbox防御已实现 |
| 7 | ✅ 通过 | Black clean, Flake8 165 warnings(历史遗留), 258/258 passed |

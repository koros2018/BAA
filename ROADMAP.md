# BAA 路线图

> 工作区快捷入口 → 完整版见 `docs/07-重大变更/工程可行性研究报告及设计（终稿）/baa-roadmap.md`

## 完成状态

> 截至 2026-08-14：P1~P105 路线图内全部完成。P95 不可行（xref 空壳问题）。P96 SFT 数据集生成完成（1290 条静态样本）。P97 黄金标准更新完成（0 漏报 0 回归）。P98 跨图纸对比报告完成（东莞通 99.6% A 级）。P99 PDF 矢量解析器完成（比例尺自动恢复，全链路验证）。P100 PDF 接入主流程完成（DrawingParser.parse() 直接吃 PDF，8 测试全绿）。P101 扫线法房间检测完成（左手定则平面面遍历，T-junction 分割，外框面剔除，LWPOLYLINE 直接提取，2068/2068 测试全绿）。P102 基线重建完成（T3 空壳过滤，data/files/ 有效 DXF + 东莞通建筑图）。P103 PDF 房间检测修复（轴对齐合并 + 全量 primitives 扫线法，room 0→14）。P104 房间面过滤增强（外框面剔除/双线墙间隙过滤/bbox 重叠去重，8 faces→2 有效 room）。P105 走廊独立识别+门洞检测（_classify_face 宽高比+短边，29 room + 16 corridor + 19 doorway）。P106 YOLO 伪标签管线（179 张标注 / 22051 标签 / 10 类）。P107 扫线法→EVAC 闭环（doorway 参与拓扑+BFS+gap<800mm 瓶颈）。P108 门洞→DIM 自动触发（DIM-006/DIM-009 target_entities 加 doorway，gap_width_mm 转换）。

> 截至 2026-08-15：P111~P118 为按价值排序的下一步详细开发方案。P111/P112/P113 为模型与检测质量闭环；P114/P115 为用户系统与协作落地；P116/P117 为部署与工程化；P118 为产品化验证。

### v1.8.3~v1.8.5 — 真实图纸走廊宽度推断
- ✅ `_compute_bbox` 4层兜底（LWPOLYLINE坐标修复）
- ✅ 平行线聚类算法（走廊宽度覆盖100%）
- ✅ 图层规则全词匹配（消除2536个假door）
- ✅ NaN防御 + ARC→door宽度推断
- ✅ 最终基线：泵房0/202，配电房0/210，室外电气12/404，东莞通16/540

### v1.8.6 — 合成图纸检出率100%
- ✅ 生成器修复（LIGHT-001/DIM-007/ATTR-002实体补齐）
- ✅ 626/626违规检出，19个原子函数全部100%
- ✅ 测试修复（conftest + 导入路径）

### v1.9.0~v1.9.5 — API密钥管理
- ✅ `api_key_manager.py`：436行，密钥全生命周期管理
- ✅ 6个admin端点（创建/列表/撤销/轮换/统计/删除/验证）
- ✅ 前端密钥管理页面（统计卡片+密钥表格+创建/详情/撤销）
- ✅ 侧边栏「系统设置」→「连接配置」
- ✅ 连接配置页：后端服务 + 访问令牌（手动添加/从密钥管理导入）
- ✅ 密钥管理页独立持令牌，解耦连接配置
- ✅ 多worker密钥加载修复
- ✅ 75/75测试全部通过

### v1.10.0 — 前端体验优化
- ✅ 审查历史记录页（搜索/筛选/详情弹窗/清空）
- ✅ 概览页使用真实reviewResults数据+规范频率/违规分布柱状图
- ✅ 结果分析页增加审查趋势/违规分布可视化+建筑类型/时间列
- ✅ 图纸渲染图片支持点击全屏缩放

### v1.11.0 — 部署准备
- ✅ Dockerfile（多阶段构建，python:3.12-slim）
- ✅ docker-compose.yml（数据卷/日志轮转/环境变量）
- ✅ DEPLOY.md（部署指南）
- ✅ 健康检查端点增强（子系统状态/启动时间/degraded）

### v1.12.0 — 原子函数扩展 30/30
- ✅ 11条 L3 规范条目（防火间距/排烟窗/消防电梯等）
- ✅ 22个 L3 原子函数测试（全部PASS/FAIL双覆盖）
- ✅ 97/97测试全部通过

### v1.24.2 — 质量提升：door属性推断 + 小门排除 + TEXT误分类修复
- ✅ TEXT 实体误分类为 fire_door 修复（需完整匹配"防火门"）
- ✅ Polygon 类 door 宽度推断增强（闭合多边形用长边推断宽度）
- ✅ DIM-006/DIM-009 增加小门排除（<0.8m 不适用疏散门/出口宽度判定）
- ✅ ATTR-001 防火门等级判定改为 >=（至少丙级），target 移除 door
- ✅ 真实图纸违规从 414 → 120（降幅 71%）

### v1.28.0 — DWG T3 格式支持 + 代码质量提升
- ✅ DWG T3 格式六级兜底解析（aspose-cad → ezdwg export_dxf → LibreCAD → manual → raw decode）
- ✅ aspose-cad SIGABRT 子进程隔离 + DOTNET 环境变量
- ✅ LibreCAD CLI 禁用 + xvfb-run 无头环境支持
- ✅ pytest markers 注册（slow/api）+ 默认跳过慢测试
- ✅ 注释清理（3460 处冗余 IDE 注释）
- ✅ 真实图纸违规：120 → 119

### v1.28.1 — 真实图纸违规降噪（DIM-004/DIM-006 误报消除）
- ✅ DIM-004 走廊宽度合理性检查（转换后 <0.5m 跳过 YOLO 误检）
- ✅ DIM-006 疏散门净宽边界容差（±2% 测量误差跳过）
- ✅ EVAC-001 大面积 room 跳过（area > 5000）
- ✅ 真实图纸违规：119 → 118（-2 误报）

### 后续方向（按价值排序）

> ⚠️ **2026-08-17 复盘后优先级重置**（基于 docs/复盘_20260817.md）：
> P111~P118 原排序中 P111（YOLO训练）已被证伪（mAP50=0.001），P112~P118 顺序重排。
> 新增 P119~P123 为复盘识别的 P0/P1 级缺口。
> **停止堆原子函数数量，停止YOLO训练，先修工作流闭环+异常处理+大文件能力。**

| 优先级 | 事项 | 状态 |
|--------|------|------|
| ~~P7-P40~~ | 已完成 | ✅ |
| P119 | 违规审核工作流（确认/误报/待核实） | 🔴 未启动 |
| P120 | 异常处理全量修复（pass空吞/裸except） | 🔴 未启动 |
| P121 | 大文件解析（结构图SKIP44%+大图SKIP18%） | 🔴 未启动 |
| P122 | 前端安全修复（XSS/innerHTML/测试框架） | 🔴 未启动 |
| P123 | 前端架构重构（Vite+TS+组件化） | 🔴 未启动 |
| P112 | 用户认证、团队空间与协作前端 | 🟡 进行中（401修复完成，工作流待续） |
| P113 | 真实用户试用与黄金标准扩库 | 🟡 可启动（依赖P119） |
| P116 | 报告产品化（封面/签字/整改通知单） | 🟡 可启动 |
| P114 | 前端架构重构（含E2E测试） | 🟡 含在P122/P123中 |
| P115 | 云端部署流水线与生产配置 | 🟢 后续 |
| P117 | 模型参数、规范版本与知识库开放 | 🟢 后续 |
| P118 | 商业模式与产品定位验证 | 🟢 后续 |
| ~~P111~~ | ~~YOLOv8m训练~~ | ❌ **已证伪，终止** |

### P7: 真实图纸验证
- 收集更多真实图纸（消防/电气/建筑各10+张）
- 建立各类型真实图纸的合规基线
- 针对真实图纸优化解析和审查精度
- 目标：真实图纸检出率 > 70%

### P8: 多文件批量审查
- 支持一个项目多个图纸文件同时审查（最多20个）
- 跨图纸实体关联分析（如疏散路线连通性）
- 批量对比报告（PDF导出）

### P9: EMA2 第三方对接
- REST API 对接标准（异步任务模式）
- Webhook 回调通知
- 批量任务队列支持

### P10: 用户反馈闭环
- 用户可对审查结果申诉
- 申诉数据自动入库
- 基于申诉数据的模型微调
- 反馈统计与阈值调整建议

---

## 质量提升 & 补短板工作计划

### P21: 多线程原子函数超时保护
- **背景：** 大图纸（>50MB）或异常实体导致原子函数卡死，阻塞整条审查链路
- **方案：** 每个原子函数运行增加超时控制（默认 30s，可配置），超时标记为 degraded 状态并跳过，不影响其他原子函数
- **优先级：** 高（影响整体稳定性）

### P22: 多边形房间分割语义检测
- **背景：** 部分图纸房间轮廓非标准闭合多边形（如有缺口的 L 形/U 形房间），当前检测会遗漏或分割错误
- **方案：** 引入多边形简化 + 缺口补全算法，对非凸房间进行语义分割后再检测
- **优先级：** 中高

### P23: 建筑类型检测缺失（混合建筑场景）
- **背景：** 商住混合、地下室+塔楼等组合建筑，单一建筑类型推断导致规范匹配偏差
- **方案：** 支持多建筑类型并行匹配，按楼层/区域划分后分别适用规范
- **优先级：** 中

### P24: room 识别覆盖率（疑难布局/不规则房间）
- **背景：** 部分真实图纸中的异形房间（弧形、斜墙、多段线复合）room 识别率低
- **方案：** 增加多段线/ARC 房间识别能力，结合 bounding box 近似推断
- **优先级：** 中高

### P25: YOLO 误检规则层兜底（走廊/门/窗）
- **背景：** YOLO 模型在复杂背景图中仍有一定误检，当前规则层兜底只覆盖了走廊宽度 <0.5m 跳过
- **方案：** 增加更多规则层后置校验（门方向、窗墙体对齐、走廊连续性），降低 YOLO 误检对最终违规的影响
- **优先级：** 高

### P26: 中文标准扩展（《建筑设计防火规范》修订）
- **背景：** 基于 GB 50016-2014(2018年版) 文本，补充防火规范原子函数覆盖空白
- **已扩展（4条）：** LIGHT-002（楼梯间照度5.0lx）/ DIM-011（消防车道净高4m）/ DIM-012（避难走道净宽）/ DIST-004（救援窗口间距20m）
- **待做（2025修订网络可查时）：** 对比2025修订版新增/变更条款，补充原子函数
- **优先级：** 中

### P27: 批量审查项目级别汇总报告
- **背景：** 当前批量审查只输出逐文件结果，缺少跨文件的统一违规汇总
- **方案：** 增加项目级汇总视图（按规范条目、严重级别、建筑类型分类统计）
- **优先级：** 中（待做）

### P28: 复杂 DWG（外部参照/xref/块嵌套）解析鲁棒性
- **背景：** 含外部参照（xref）或深度块嵌套的 DWG 文件，当前解析链可能不完整或丢失实体
- **方案：** 增加 xref 解析支持（自动追踪引用文件）、块嵌套深度展开、合并外部参照实体
- **优先级：** 中高（待做）

### P29: 原子函数断言可读性/错误信息优化
- **背景：** 部分原子函数失败时错误信息不明确，难以定位根因
- **方案：** 统一断言信息格式（预期值/实际值/位置坐标），增加上下文字段到审查日志
- **优先级：** 中（待做）

### P30: 并发审查资源隔离与排队
- **背景：** 多用户并发审查时资源争抢可能导致 OOM 或响应超时
- **方案：** 实现审查任务队列（基于 Redis 或内存队列），限制最大并发数，按用户/优先级分配资源
- **优先级：** 中高

---

## v1.30.0 路线图（P31~P40）

### P31: 质量提升 — 审查缓存持久化
- **背景：** 当前三层缓存为内存缓存，服务重启后丢失，相同图纸需重新解析
- **方案：** 引入 SQLite/Redis 持久化缓存，支持文件哈希 → 解析结果缓存
- **预期收益：** 重复图纸审查耗时 0.16s → 0.01s（缓存命中即返回）
- **优先级：** 高（直接影响用户体验）

### P32: 质量提升 — 原子函数链式依赖解析
- **背景：** 当前原子函数独立执行，部分函数结果可复用（如 AREA-001 与 DIM-002 都需面积）
- **方案：** 引入依赖图，函数间共享提取值，减少重复计算
- **预期收益：** 原子函数阶段耗时降低 30-50%
- **优先级：** 中

### P33: 补短板 — 疏散路径连通性验证
- **背景：** 当前仅检查疏散路径存在性（EVAC-001），不验证实际连通性
- **方案：** 基于走廊拓扑图 + BFS 路径搜索，验证 room→楼梯间/出口的路径可达
- **预期收益：** 真实图纸疏散分析从"有路径"到"路径可达"
- **优先级：** 高（安全关键功能）

### P34: 补短板 — 设备类实体识别（消防/电气）
- **背景：** 当前实体识别以建筑结构为主（wall/door/window/room），消防/电气设备几乎无识别
- **方案：** 增加消防设施（灭火器/消火栓/喷淋头）、电气设备（配电箱/应急照明）的图例匹配
- **预期收益：** 当前 100+ EXIST 假阳性可大幅降低
- **优先级：** 高（核心降噪）

### P35: 补短板 — 多层/多区域图纸解析
- **背景：** 多层建筑图纸常分层绘制，当前解析将所有实体混在一起，无法按楼层分区
- **方案：** 增加楼层分隔线/标高检测，按楼层拆分实体，支持逐层审查
- **预期收益：** 多楼层图纸的精确逐层审查
- **优先级：** 中

### P36: 质量提升 — 审查结果评分与置信度
- **背景：** 当前审查结果二元判定（PASS/FAIL），缺少置信度和严重度量化
- **方案：** 引入评分机制（0-100），基于实体识别率、属性完整度、YOLO 置信度综合评分
- **预期收益：** 用户可区分"确认违规"和"可能违规"
- **优先级：** 中

### P37: 质量提升 — 批量审查并发 + 进度通知
- **背景：** 当前批量审查串行执行，大批量文件耗时较长且无进度反馈
- **方案：** 批量审查引入并发处理 + WebSocket/SSE 进度推送
- **预期收益：** 20 文件批量审查从 ~40s 降至 ~10s
- **优先级：** 中

### P38: 补短板 — 图例/图块知识库扩展
- **背景：** 不同设计院的图例差异大，当前硬编码图例匹配覆盖不足
- **方案：** 构建可配置的图例知识库（JSON/YAML），支持用户自定义图例映射
- **预期收益：** 新图纸类型无需改代码即可适配
- **优先级：** 中低

### P39: 质量提升 — CI/CD 流水线
- **背景：** 当前无自动化 CI，代码合并依赖手动测试
- **方案：** 配置 GitHub Actions（pytest + lint + type check）
- **预期收益：** 代码质量自动化保障
- **优先级：** 中低

### P40: 补短板 — 多语言报告导出
- **背景：** 当前 PDF 报告仅中文，国际用户无法使用
- **方案：** 支持英文/日文/韩文报告导出，基于模板引擎 + i18n 字符串表
- **预期收益：** 国际市场可用性
- **优先级：** 中低

---

## v1.31.0 方向（P41~P50 草稿）

| 编号 | 方向 | 说明 |
|------|------|------|
| P41 | AI 辅助修正建议 | 基于 LLM 生成违规修正方案 |
| P42 | 图纸版本对比（PDF 报告集成） | ✅ 已完成 |同一项目前后版本自动 Diff |
| P43 | 多用户协作 | 团队审查 + 评论 + 审批流 |
| P44 | 规范合规度评分（报告页） | ✅ 已完成 |建筑整体合规度评分报告 |
| P45 | 能耗分析集成 | ✅ 已完成 |围护结构热工性能计算（16条原子函数 + K值反算 + 多材料复合层）|
| P46 | 结构荷载验算 | ✅ 已完成 |16条原子函数 + 16条修正模板 + 64单元测试 |
| P47 | 无障碍设计检查 | ✅ 已完成 |轮椅通道/坡道/电梯检查 |
| P48 | 施工图审查标准 | ✅ 完成（后端 30条CD项 + API + SDK，前端 P92） |
| P49 | 云端部署优化 | ✅ 已关闭（P63 K8s部署方案覆盖） |
| P50 | API 文档 SDK | OpenAPI 自动生成 SDK |

---

## v2.6.0 方向（2026-07 复盘确定，P51~P59）

> 源自 2026-07-13 开发复盘 `docs/retrospective-20260713.md`
> 核心转向：从"加功能"到"做精度"，从"正向解析"迈向"反向重构"

### 近期（已完成，2026-07-13）

**P51: 原子函数"属性缺失"审计** ✅
- **成果：** 6 个函数修复，FAIL 从 910→6（↓99.3%）

**P52: 真实图纸黄金标准标注** ✅
- **成果：** 41 CONFIRMED / 0 SKIP，100% 精确率

**P53: 审计脚本纳入 CI** ✅
- **方案：** GitHub Actions 自动审计（`.github/workflows/audit.yml`），push/PR + 每周一

**P54: 疏散连通性分析重做** ✅
- **成果：** BFS >120s→1.51s，EVAC 系列从 SKIP→CONFIRMED

**P55: 平面图布局分析优化** ✅
- **成果：** 2.1电气 272→27 room，6.火灾自动报警 10→6 room

### 中期（7/13~7/14 完成）

**P56: 反向重构原型（简易场景）** ✅
- **成果：** 约束推理→DXF→正向闭环 0 FAIL

**P57: 规范知识图谱扩展至 200+ 原子函数** ✅
- **成果：** 总数 340→390，覆盖 GB50016/50763/50974/50116，容量满

**P58: 反向重构支持完整建筑平面图生成** ✅
- **成果：** MultiRoomEngine + 疏散路径生成（EVAC 箭头线）

### 近期（7/20）

**P59: 真实图纸验证体系常态化** ✅
- **成果：** 8/8 真实图纸测试基线全绿，±30% 宽容度稳定
- **覆盖：** 6 张基线图纸 + 全部真实 DXF 解析 + 消防设施检测

**P11: 原子函数全量单元测试** ✅
- **成果：** 1563/1563 测试全绿，修复 6 个根因问题

**P12: 清理 __pycache__** ✅
- **成果：** 585 个 __pycache__ + 4128 个 .pyc 清理，.gitignore 补强

**P13: Dockerfile 版本同步** ✅
- **成果：** LABEL version 2.5.4→2.5.11

### 当前版本

- **v2.5.15-stable**（2026-07-22，`26f0150`，本地 tag 已打）
- **HEAD**（`9d1a002`，v2.5.15-stable 后 2 commits）
- 全量测试 1975/1975 ✅，black 76 文件 ✅，JS 语法正确 ✅
- 原子函数 422（dim:124, exist:128, dist:34, count:30, area:26, 等）

### 后稳定版改进（v2.5.13-stable → v2.5.15-stable）

| 改动 | 内容 |
|------|------|
| P45/P46 修正模板补全 | THERM 16 条 + STR 16 条（原仅 5 条） |
| manager.py BOM 修复 | 移除 39 条孤立 docstring |
| P46 前端面板 | 11 种结构构件验算 + STR 审查联动 |
| 置信度可视化 | 后端 4 处 detail 补 confidence，前端违规卡置信度条 |
| STR 违规汇总表 | 按 func_id 分组，低置信度优先，彩色进度条 |
| spec_repository 重构 | 3528→32 行，拆为 spec_data/ 6 个标准分片 |
| 批量/对比/渲染路由 | details 补 func_id/confidence/severity |

### 后稳定版改进（v2.5.15-stable → HEAD）

| 改动 | 内容 |
|------|------|
| P60 修正建议自动触发（9d1a002） | review_routes.py 修 query_params/undefined → os.environ.get 默认 'rule'；前端审查完成后自动展开修正建议面板，从后端 result.corrections 直接渲染 |
| api_globals __all__ 补全（6d42c13） | 补 generate_file_id/store_file，修复 pre-existing test failures |
| P61 置信度分级语义（de9a4e0） | detail 补 confidence_tier 字段 + summary 分级统计；前端标签语义化 + 摘要分级卡片 |

### 下一步（P60+ 产品力提升，2026-07-22 启动）

> 核心转向：从"做精度"到"做产品力"，打通用户看到违规→知道怎么改的最后一公里

| 编号 | 方向 | 说明 | 状态 |
|------|------|------|------|
| **P60** | **修正建议前端接入** | 审查接口自动注入 corrections（rule 模式），违规卡/违规列表/对比页内联展示💡修正建议，审查完成后自动展开修正建议面板 | ✅ 完成 |
| **P61** | **置信度分级语义** | 高/中/低→确认违规/疑似违规/建议复核，报告页汇总分级统计，消除"置信度"黑箱建立用户信任 | ✅ 完成 |
| **P62** | **审查报告结构化摘要** | 违规 TOP-5 核心问题 + 整改优先级排序 + 合规路径指引，让报告从"清单"变成"建议" | ✅ 完成 |
| ~~**P62**~~ | ~~审查报告结构化摘要~~ | ~~违规 TOP-5 核心问题 + 整改优先级排序 + 合规路径指引~~ | ~~待做~~ |
| **P63** | **云端部署方案** | K8s + HPA + 多副本 + 共享对象存储，支撑多租户生产部署 | 待做 |
| **P64** | **OpenAPI 自动生成 + SDK** | OpenAPI 自动生成 Python/JS SDK，降低第三方集成门槛 | ✅ 完成 |
| **P65** | **LLM 修正建议生成** | 基于违规类型+图纸上下文动态生成修正建议，模板兜底，覆盖率从 32 条→动态无限 | ✅ 完成 |
| **P66** | **规范版本管理** | GB50016 2025 修订版 + 自动条款对照，解决"审查标准跟不跟得上" | ✅ 完成 |
| **P67** | **反向重构 DXF→DWG 导出** | 复杂布局求解 + DWG 格式导出，从"审查工具"走向"设计辅助" | ✅ 完成 |
| **P68** | **行业案例库** | 典型图纸审查报告合集（可检索），营销素材 + 用户教育 | ✅ 完成 |

---

### P69+ 方向（产品力持续，2026-07-23 起）

| 编号 | 方向 | 说明 | 状态 |
|------|------|------|------|
| **P69** | **PDF 报告导出** | ✅ 2026-07-24（`d3474a9`，含违规清单/修正建议/结构化摘要） |
| **P70** | **设备类实体识别**（P34 延续） | 消防/电气/给排水设备识别，消防泵/喷淋/消火栓自动核查，设备类违规归零 | ✅ 2026-07-24 |
| **P71** | **批量审查 Webhook 通知** | ✅ 2026-07-24（`c7f49b6`，支持 Jenkins/GitLab CI/飞书/钉钉） |
| **P72** | **审查统计仪表盘** | API 调用量/图纸量/违规分布/置信度趋势/修正采纳率，运营决策依据 | ✅ 2026-07-26 |
| **P73** | **多区域/多图层图纸解析**（P35 延续） | 同一 DXF 内多个 Sheet 分区独立审查，结果聚合为项目级报告 | ✅ 2026-07-26 |

---

## P74-P78 准确率专项提升（2026-07-27 起）

> 来源：16 张真实图纸 P0 Benchmark 报告
> 核心转向：从"做产品力"回到"做准确率"——86% 的审查结果是误报和漏检

### 📊 Benchmark 核心数据

| 指标 | 现状 | 根因 |
|------|------|------|
| 未分类图元 | **32%（~21,000）** | LAYER_RULES 覆盖严重不足 |
| P70 定义但未检出实体类型 | **74.5%** | 代码定义≠实际识别 |
| EXIST-052/083/093 误报 | **100% FAIL** | 3 个函数占总 FAIL 97.7% |
| 9 项黄金标准漏检 | **全部漏检** | 核心违规完全没查到 |

---

### 🥇 P74 语义识别规则层全覆盖（Layer Rules）
**目标**：未分类图元 32% → <10%，真实图纸实体检出率 25.5% → >70%

**具体动作**：
- 分析 16 张真实图纸中 21,000 个未分类图元的层名/类型/属性分布
- 补充 LAYER_RULES（层名 → 实体类型映射表），覆盖 fire_wall / fire_curtain / fire_lane / staircase 等高频层
- 增加 TEXT 模式兜底（图层名 + 文字内容联合推断，解决"层名乱写但文字标注清晰"的场景）
- 产出：16 张图纸未分类率下降，检出实体类型数量翻倍

---

### 🥇 P75 EXIST-052/083/093 误报根因修复
**目标**：3 个 100% FAIL 的函数归零，FAIL 总数从 8,659 降到 <500（降幅 94%）

**根因**：
- **EXIST-052**（防火墙门窗洞口）：door/fire_wall/opening/window 全部被当违规，`requires_global_context=True` 函数在缺少目标实体时应 PASS 而非 FAIL
- **EXIST-083/093**（消防救援窗）：rescue_window 识别不足 + 阈值 `≥2` 不合理（单窗也算达标）

**具体动作**：
- 修复 `requires_global_context=True` 函数在目标实体缺失时的判定逻辑
- 调整 EXIST-083/093 阈值：按防火分区聚合而非按图判定
- 预期：FAIL 从 8,659 降到 <500

---

### 🥈 P76 真实图纸黄金标准漏检修复
**目标**：9 项核心违规至少检出 5 项

**根因**：EVAC（疏散距离）/ DIM（门宽）/ DIST（防火间距）等核心原子函数在复杂图纸上图元推断失败

**具体动作**：
- 逐项分析 9 项黄金标准漏检的根因（图元缺失 vs 阈值过严 vs 拓扑推理失败）
- 针对性修复，建立"黄金标准通过率"作为 CI 门禁指标

---

### 🥈 P77 原子函数容量扩容 + 新标准覆盖
**当前**：422 个函数，覆盖 GB50016 / GB50974 / GB50116 / GB50763

**缺口**：
- GB50016-2014（2018 版）仍有空白条款
- GB55036-2022（防火新规范）未覆盖
- GB51096-2015（住宅设计规范）未覆盖

**具体动作**：
- 扩展容量上限（当前 422 槽位已用满，需扩容）
- 补充新标准原子函数（GB55036 / GB51096）
- 优先覆盖与 P0 benchmark 漏检相关的条款

---

### 🥉 P78 质量提升工程
**目标**：工程卫生，降低后续迭代门槛

**具体动作**：
1. **大文件拆分**：review_routes.py（1600+ 行）→ 拆分为 multi_sheet / standard / thermal 三个子路由
2. **test_webhook.py 对齐**：当前测试引用旧 API 端点，需更新到 `_dispatch_webhooks` 新版
3. **P73 复盘文档**：`docs/P73_复盘.md`——记录 to_dict 序列化陷阱和闭包 import 遗漏两个坑，写入开发规范

---

## P79-P83 准确率冲刺（2026-07-27~2026-07-30，已完成）

> 来源：P78 之后实测数据持续暴露的问题
> 核心：P82 违规降噪 + P83 几何兜底回归修复，真实图纸违规从 124→0

| 编号 | 方向 | 成果 | 状态 |
|------|------|------|------|
| **P79** | LAYER_RULES 补全 | 7 张 DXF 审计，520 层名未匹配 148→5（71.5%→99.0%） | ✅ 2026-07-29 |
| **P80** | 疏散漏检修复 | 泵房 6 rooms 6/6 has_route，几何兜底短路修复 | ✅ 2026-07-29 |
| **P81** | 真实图纸验证常态化 | CI baseline_verify job，6 张固定图纸 ±30% 容忍度 | ✅ 2026-07-29 |
| **P82** | 违规降噪 | 真实图纸 6 张 124→0 FAIL（100% 降噪），15% 容差 + SB 误杀修复 | ✅ 2026-07-29 |
| **P83** | 几何兜底回归修复 | 恢复 P80 原始逻辑，A1 door 2268→23 回归修复 | ✅ 2026-07-30 |

---

## v2.7.0 方向（P84+ 产品力提升，2026-07-30 起）

> 排序原则：准确率根因 > 工程迭代门槛 > 用户体验 > 新能力 > 生态集成
> 当前版本基线：v2.5.27-stable（`766347f`），真实图纸违规 0，测试 2006/2020

### P84: YOLO 模型重训练（准确率根因）
**目标**：真实图纸召回率从稀疏 0.19% 非白像素场景下的低召回，提升到可接受水平

**背景**：当前 YOLOv8n 在真实图纸上检测数仅 6（conf=0.25），而训练数据密集（30-50% 非白像素）。conf 降至 0.15 后检测数增至 64，但多数为误检。根因是训练数据与真实图纸的分布差异。

**具体动作**：
1. 渲染更多真实图纸（至少 20 张，覆盖多种图纸类型）生成训练集
2. 用 labelImg 标注 18 类实体（优先标注高频类：door / window / room / stair / fire_hydrant / sprinkler）
3. 增量训练（fine-tune）现有 YOLOv8n，而非从头训练
4. 评估：mAP50 提升 + 真实图纸召回率提升
5. 与当前模型对比，决定是否替换

**验收标准**：
- 标注集 ≥20 张，覆盖 ≥5 种图纸类型
- 真实图纸上高频类（door/window/stair）召回率 ≥50%
- mAP50 对比基线（yolov8n.pt）有提升

---

### P85: 大文件/技术债重构（迭代门槛）
**目标**：降低后续开发迭代门槛，消除大文件维护负担

**背景**：`semantic_analyzer/main.py` 3303 行、`report_generator.py` 1216 行仍是单文件瓶颈，修改风险高。

**具体动作**：
1. `semantic_analyzer/main.py`（3303 行）→ 拆分为 classify.py / context.py / aggregation.py
2. `report_generator.py`（1216 行）→ 拆分为 sections/ 子目录（overview / violations / corrections / summary）
3. 对外接口（SemanticAnalyzer / ReportGenerator 类）不变，仅内部重组
4. 每个子模块 ≤500 行
5. 测试全通过

**验收标准**：
- 主文件拆完后每子模块 ≤500 行
- 测试 2006/2020 全通过
- 无 API 行为变更

---

### P86: 前端骨架屏/进度条/统一 Toast（用户体验）
**目标**：消除纯字符串拼接的视觉混乱，审查过程有视觉反馈

**具体动作**：
1. 骨架屏：审查历史/结果/概览页占位条 ✅
2. 审查进度条：审查触发后显示进度，完成显示 ✓/✗ 摘要 ✅
3. 统一 `showToast(message, type)` 组件，替换所有内联 alert ✅
4. 模板字符串标准化：内联 HTML 拼接 → `buildRow()` / `buildCard()` 命名函数

**验收标准**：
- 审查历史/结果页有骨架屏 ✅
- 审查过程有进度条 ✅
- 统一 toast 组件，无内联 alert ✅

---

### P87: 批量审查并发优化（性能）
**目标**：大批量（20+ 文件）审查耗时显著降低，有进度反馈

**具体动作**：
1. 批量审查引入线程池并发（当前串行）
2. 进度推送（SSE 或轮询），前端实时显示每文件状态
3. 资源隔离：限制最大并发数，避免 OOM
4. 单文件失败不阻塞整体

**验收标准**：
- 20 文件并发审查比串行快 ≥50%
- 前端有逐文件进度反馈
- 50MB+ 文件不触发 OOM

---

### P88: DXF→DWG 反向重构闭环（设计辅助能力）
**目标**：反向重构产物可导出为 DWG，从"审查工具"走向"设计辅助"

**背景**：P67 已做 DXF→DWG 导出原型，但未成熟。反向重构（P56/P58）生成 DXF，但无法导回 DWG。

**具体动作**：
1. 整理 P67 原型代码，确认当前状态
2. 利用 ezdwg / LibreCAD 导出 DWG 格式
3. 反向重构 DXF → DWG 端到端流水线
4. 验证：导出 DWG 可用 LibreCAD/AutoCAD 打开

**验收标准**：
- 反向重构 DXF 可导出为 DWG
- DWG 文件可用 LibreCAD 打开

---

### P89: OpenAPI 自动生成 + SDK（生态集成）
**目标**：降低第三方集成门槛，自动生成 Python/JS SDK

**具体动作**：
1. FastAPI 自动生成 OpenAPI 3.1 文档
2. 用 openapi-generator 生成 Python + JS SDK
3. 集成到部署文档（DEPLOY.md）
4. CI 自动生成 + 发布到 GitHub Release

**验收标准**：
- OpenAPI 文档可通过 `/docs` 访问
- Python SDK 可 pip 安装使用
- JS SDK 可通过 npm 安装使用

---

### P93: 施工图审查模型参数导出（垂直大模型微调数据生产）✅（2026-08-09）
**目标**：将 BAA 审查引擎的判定规则、阈值参数、审查样本以结构化格式导出，供下游开源大模型（Qwen / DeepSeek / 等）做垂直领域 SFT / RLHF 微调数据准备。

**已完成交付**：
- 后端：`model_params/exporter.py`（374 行）+ `model_params_routes.py` 6 端点
- 前端：`baa-model-params.js`（190 行，5 Tab：functions/layer-rules/cd-items/samples/export）
- SDK：6 个新方法（functions/layer-rules/cd-items/samples/spatial-graph/export）
- 测试：22/22 全绿（`test_model_params.py` 14 + `test_sdk_model_params.py` 8）
- 格式支持：json/jsonl-sft/hf-dataset/csv
- 8 commits: exporter+routes → 路由注册 → SDK → 5轮前端修复

---

**背景**：P48/P92 已构建 30 条施工图审查标准（CD），但其作为静态规范清单，产品价值有限。核心资产不是清单本身，而是 BAA 审查引擎积累的规则逻辑、判定参数、空间关系特征——这些才是开源大模型在建筑垂直领域微调时最稀缺的知识来源。

**具体动作**：

#### 1. 原子函数参数表导出（`/api/v1/model-params/functions`）
- 输出 422 个原子函数的完整参数结构：
  - 元信息：func_id, title, standard_ref, level(L1/L2/L3), major
  - 判定参数：target_entities, thresholds, tolerance, spatial_constraints
  - 规则逻辑：check_method(auto/manual/ai), global_context, chain_dependencies
- 格式：JSON 数组，每项一个函数

#### 2. LAYER_RULES 语义映射导出（`/api/v1/model-params/layer-rules`）
- 输出 567 + 90 条图层→实体类型映射
- 包含：layer_pattern, entity_type, priority, source
- 这是建筑图纸领域最稀缺的垂直知识库，直接可用于训练图纸解析模型

#### 3. 审查样本导出（`/api/v1/model-params/samples?limit=&review_id=`）
- 从已完成审查中提取（输入特征 → 判定结果 → 判定依据）三元组
- 输入特征：实体类型、尺寸、空间关系、图层信息
- 判定结果：PASS/FAIL/CONFIRMED
- 判定依据：触发的原子函数、违规描述、规范条款引用
- 支持按 review_id 导出单次审查，或全局最近 N 条

#### 4. 空间关系图导出（`/api/v1/model-params/spatial-graph?review_id=`）
- room↔wall↔door↔stair↔exit 的邻接、距离、包含关系
- 格式：节点列表 + 边列表，含几何属性（bbox, distance）
- 用于训练模型理解建筑空间拓扑

#### 5. 输出格式兼容（`/api/v1/model-params/export?format=`）
- `json`: 原始结构化 JSON（默认，最灵活）
- `jsonl-sft`: OpenAI/Qwen/DeepSeek 通用 SFT 格式：
  ```
  {"messages": [{"role": "user", "content": "<输入特征>"}, {"role": "assistant", "content": "<判定结果+依据>"}]}
  ```
- `hf-dataset`: HuggingFace Dataset 格式（Arrow 或 parquet 描述）
  - 字段：features, targets, metadata(standard_ref, level, major)
  - 兼容 `datasets.load_dataset()` 直接加载
- `csv`: 表格格式，Excel 直接打开

#### 6. 前端面板（复用 `page-cd`，改造 `baa-cd.js`）
- 顶部 Tab：`参数表 | 语义映射 | 审查样本 | 空间关系 | 导出` 
- 每 Tab 展示对应数据（表格 + 统计）
- "导出" Tab：选择格式 + 范围 → 下载
- 支持 API key 鉴权（审查样本含真实图纸信息）

**后端实现规划**：
- 新文件：`src/api/model_params/model_params_routes.py`（路由 + 格式化）
- 新文件：`src/baa_engine/model_params/exporter.py`（数据提取 + 格式转换）
- 新文件：`src/baa_engine/model_params/layer_rules_export.py`（语义映射导出）
- 注册到 `baa_api.py`：`app.include_router(model_params_router, prefix="/api/v1/model-params")`

**前端实现规划**：
- 改造 `baa-cd.js` → 重构为多 Tab 面板
- 保留 P48 后端（CD 规范列表，仍可访问，作为参考页）
- 侧边栏名称改为 `🤖 模型参数`

**SDK 扩展**：
- `list_function_params()` — 原子函数参数表
- `export_layer_rules()` — LAYER_RULES 语义映射
- `export_review_samples(review_id, format)` — 审查样本
- `export_spatial_graph(review_id)` — 空间关系图
- `download_model_params(format, **kwargs)` — 统一导出端点

**验收标准**：
- `GET /api/v1/model-params/functions` 返回 422 个原子函数参数（JSON）
- `GET /api/v1/model-params/layer-rules` 返回 657 条语义映射（JSON）
- `GET /api/v1/model-params/samples?limit=10` 返回 SFT 样本（三元组格式）
- `GET /api/v1/model-params/export?format=jsonl-sft&limit=100` 输出 Qwen/DeepSeek 兼容 SFT JSONL
- `GET /api/v1/model-params/export?format=hf-dataset&limit=100` 输出 HuggingFace Dataset 描述
- 前端页面打开正常，各 Tab 数据加载无错误
- SDK 5 个新方法全部可用
- 测试：`test_model_params.py` ≥15 测试全绿
- 2036+ 回归测试无失败
---

## P105 — 扫线法走廊/门洞检测（2026-08-13，commit `3afaacd` ✅）

### 走廊面分类

在 `_sweep_line_detect_rooms` 和 `_sweep_and_filter` 的过滤循环中，每通过 P104 过滤的面调用 `_classify_face()` 判断类型：

- **走廊 (corridor)**：宽高比 ≥ 3.0 **且** 短边 ∈ [2000, 4000]mm
- **房间 (room)**：其他通过过滤的面

走廊 entity 额外携带属性：
- `clear_width` (m) — 净宽 = 短边 ÷ 1000
- `length` (m) — 长度 = 长边 ÷ 1000

### 阈值确定依据

实测 45 个扫线法面（12 个走廊 / 33 个房间）：

| 分组 | 短边中位数 | 短边范围 | 宽高比中位数 | 面积中位数 |
|------|-----------|---------|------------|----------|
| corridor | 2700mm | [2252, 3407] | 6.88 | 13m² |
| room     | 4955mm | [2330, 32998] | 1.83 | 36m² |

走廊短边上限 3000mm + 10% 缓冲 = 3300mm，取整到 4000mm；下限保留 P104 已有的 2000mm。

### 新增常量

```python
_CORRIDOR_ASPECT_RATIO = 3.0           # 宽高比阈值
_CORRIDOR_SHORT_EDGE_MAX_MM = 4000.0  # 走廊最大宽度
_CORRIDOR_SHORT_EDGE_MIN_MM = 2000.0  # 走廊最小宽度（复用 P104）
```

### 真实图纸验证（西安特发西港 一层平面图 PDF）

| 面 | 面积 | 短边 | 宽高比 | 分类 | 走廊属性 |
|----|------|------|--------|------|---------|
| 1 | 51.72m² | 4851mm | 2.29 | room | — |
| 2 | 19.28m² | 2286mm | 3.85 | **corridor** | clear_w=2.29m, len=8.80m |

### 测试

- `test_pdf_p105_corridor_detection`：构造走廊 + 房间 → 断言 corridor/room 分类正确
- 全量 5 个 P101/P103/P105 PDF 测试 ✅
- 全量回归 2071/2072 ✅（`test_multi_page_pdf` 因 PDF 文件缺失，已有问题）

### 门洞检测

`_detect_doorway_gaps(segs)` 在 axis-align 合并后的扫线法流程中，使用**原始墙段**（非合并后）独立检测门洞间隙。

算法：
1. 过滤 >=1000mm 墙段
2. 按方向分 cluster（水平/垂直）
3. 按起始坐标排序后找相邻墙段的 gap
4. 过滤：700mm ≤ gap ≤ 2500mm

返回 `SemanticEntity(type="doorway")`，属性含 `gap_width_mm` 和 `direction`。

西安特发西港 PDF 验证：19 个门洞，宽度 819~2388mm，分布合理（1000-2000mm 占多数，2000+ 为双开门/走道开口）。

### 测试

- `test_detect_doorway_gaps` / `no_gap` / `too_wide`：3 个单元测试 ✅
- `test_sweep_line_includes_doorways`：真实图纸验证（装饰图纸 0929.dxf，≥5 个 doorway）✅
- `test_pdf_p105_corridor_detection`：走廊面分类 ✅
- PDF 模块全量 19/20 ✅（`test_multi_page_pdf` 因 PDF 文件缺失，已有问题）

---

## P106+ 下一步方向（2026-08-14，v2.5.40-stable 后）

> 排序原则：准确率根因 > 工程迭代门槛 > 产品深度 > 生态集成
> 基线：v2.5.42-stable（`fd92e2b`），P107+P108 完成，605 测试，全量回归 2087/2088（1 known-fail）

### P106 — YOLO 伪标签管线 ✅（2026-08-14）
**目标**：自动化标注管线：扫线法几何推断 → DXF 渲染 → YOLO 伪标签 → 人工复核

**成果**：
- `p106_generate_labels.py`：DXF 渲染 + 语义分析 + 世界坐标→YOLO 归一化，30s 单文件超时（SIGALRM）
- `p106_discover.py`：候选图纸发现（扫描→实体分布→排序）
- **179 张标注 / 22051 个标签 / 10/18 类**
- 主力类别：wall(8512) room(8144) window(2449) door(1438) staircase(724) shaft(449) corridor(287)
- 低频：fire_door(36) exit(9) fire_window(3)
- 缺失(8类)：exit_sign/sprinkler_system/fire_alarm/insulation/evacuation_lighting/refuge_floor/fire_lane(0)
- data.yaml + train.txt(190) + val.txt(48) + stats.json 完整
- 252 张 DXF 全扫描：183 跳过(已有标签) + 60 超时(非建筑图/系统图/详图) + 9 渲染失败
- **下一步**：人工复核标签质量 + 补充 8 类缺失标注 + fine-tune YOLOv8m

### P107 — 扫线法走廊门洞→疏散连通性闭环 ✅（2026-08-14）
**成果**：doorway 参与 KEY_ENTITY_TYPES + corridor↔doorway↔room 拓扑 + EVAC BFS 路径 + gap<800mm bottleneck，9 定向测试全绿

### P108 — 扫线法门洞→DIM 自动触发 ✅（2026-08-14）
**成果**：DIM-006/DIM-009 target_entities 加 doorway，gap_width_mm(mm)→m 转换，doorway 同 exit_door 始终判定，14 定向测试全绿

### P109 — 扫线法房间属性自动推断 ✅（2026-08-15）
**成果**：`room_type_infer.py` 14 类规则（几何+走廊+文本关键词加权），`main.py` 集成 `_infer_room_types`，16 单元测试，保守策略（仅 subtype），2115/2117

### P110 — 工程重构 + 产品力收尾 ✅（2026-08-15）
**成果**：
1. `main.py` 993 行 < 1000 阈值 ✅
2. `report_generator.py` 已拆包（8 子模块）✅
3. P86: `baa-core.js` 新增 `showSkeleton/hideSkeleton/renderSkeletonContainer`，批量面板骨架屏 HTML
4. P87: `baa-sse-batch.js` SSE 流式消费者 + `batch_routes.py` 并发限制（Semaphore(4) + 120s 超时）
5. 测试：2115/2117 ✅

---

## P119~P123 复盘驱动开发方案（2026-08-17 复盘后，按致命程度排序）

> 排序依据：docs/复盘_20260817.md 深度复盘。当前最大瓶颈不是模型能力或规则数量，而是**工作流缺失（产品层面）+ 异常处理（工程质量）+ 大文件不可用（用户场景）+ 前端不可维护（技术债）**。

### P119 — 违规审核工作流：从Demo到产品的关键一步 🔴🔴🔴

**复盘结论：** BAA 当前工作流是"上传→审查→看文本列表→结束"，行业标准是"上传→AI审查→工程师逐条审核→整改通知单→复查→归档"。没有工作流闭环，产品停留在演示Demo。

**具体动作：**
1. **违规审核数据库模型**
   - 新增 `review_items` 表：`review_id`, `entity_id`, `function_id`, `status`（pass/fail/skip/override）, `user_id`, `note`, `reviewed_at`
   - 每条违规生成独立审核记录，可逐条操作
2. **后端审核API**
   - `POST /review/{id}/items/{item_id}/verify`：确认违规（`status=confirmed`）
   - `POST /review/{id}/items/{item_id}/dismiss`：标记误报（`status=dismissed`），反馈入库
   - `POST /review/{id}/items/{item_id}/pending`：待核实（`status=pending`）
   - `POST /review/{id}/items/{item_id}/note`：添加批注
   - 审核状态变更同步更新 `review_results` 汇总统计
3. **前端审核面板**
   - 违规列表每条增加「确认/误报/待核实」按钮 + 批注输入框
   - 顶部统计：`已审核 X/总数 Y`，按状态分组筛选
   - 审核完成后可生成整改通知单（P116衔接）
4. **整改通知单生成**
   - 审核完成后，`report_generator` 输出整改清单：图纸名→违规条款→确认状态→整改建议
5. **验收标准**
   - 完整流程：上传→审查→逐条审核→整改单→导出
   - 至少 5 个后端测试 + 2 个 E2E 测试
   - 审核状态变更不改变原始审查结果，只产生覆盖记录

---

### P120 — 异常处理全量修复（成本最低，影响最大） 🔴🔴

**复盘结论：** `baa_api.py` 3处`pass`空吞，`drawing_parser.py`多处`except Exception`无log，`collab_routes.py`多处`pass`。用户看到"审查完成"但结果空白，完全不知道为什么失败。

**具体动作：**
1. **全量扫描**
   - `grep -rn "except.*:" src --include="*.py" | grep "pass"`：找出所有空吞
   - `grep -rn "except Exception" src --include="*.py"`：找出所有裸异常捕获
   - `grep -rn "except:" src --include="*.py"`：找出所有无类型异常捕获
2. **修复规则**
   - `pass` → `logger.error(f"函数名: {e}")` + 返回结构化错误响应
   - `except Exception` → 替换为具体异常类型（`FileNotFoundError`/`ValueError`/`TimeoutError`）
   - 所有异常必须 `logger.exception(e)` 至少记录 traceback
3. **用户友好错误**
   - 解析失败 → `{"error": "图纸解析失败", "detail": "文件损坏或格式不支持"}`
   - 审查超时 → `{"error": "审查超时", "detail": "图纸过大，请拆分后重试"}`
   - 空结果 → 明确提示"未检测到违规"而非空白
4. **验收标准**
   - 全仓库 `pass` 空吞 = 0
   - 全仓库 `except:`（无类型）= 0
   - 所有 `except Exception` 均有 `logger.error/exception`
   - 全量测试 2117+ 回归通过

---

### P121 — 大文件解析能力（62%图纸不可用） 🔴🔴

**复盘结论：** 135张真实图纸中59张结构图SKIP（44%）+ 25张>20MB大图SKIP（18%），62%不可用。用户上传50MB图纸直接跳过，体验极差。

**具体动作：**
1. **结构图解析策略**
   - 分析59张结构图SKIP原因：是实体类型不同（梁/板/柱）还是解析器不识别？
   - 方案A：为结构图定义独立的 LAYER_RULES（结构层名映射到构件类型）
   - 方案B：如果结构图不属于审查范围，在上传时明确分类并给出替代提示
2. **大图纸分页解析**
   - 当前已有大图纸超时保护，但直接SKIP不给用户选项
   - 增加"按图纸/楼层分页解析"选项，每页独立审查后合并结果
   - 内存监控：单实体超过阈值触发分页
3. **超时体验优化**
   - TIMEOUT 4张 → 增加用户可配置超时时间（默认30s→可选60/120/300s）
   - 超时后展示已解析的部分结果而非完全空白
4. **验收标准**
   - 结构图可用率：从0%→≥50%（可解析或明确分类）
   - 大图SKIP率：从18%→≤5%
   - 整体可用率：从30%→≥70%
   - 全量测试 2117+ 回归通过

---

### P122 — 前端安全修复 + 测试框架 🔴

**复盘结论：** `baa-review.js` 72处`innerHTML+=`、`baa-ext.js` 25处，XSS防护为零，0个前端测试，`alert()`残留。每次改前端都在赌不会炸。

**具体动作：**
1. **XSS防护**
   - 引入 `escHtml()` 函数，所有动态插入HTML的内容必须经过转义
   - 优先修复：违规列表、用户输入、审查结果文本、批注内容
   - 禁止直接用 `innerHTML += userContent`
2. **消除 `alert()`**
   - `grep -rn "alert(" src/frontend/` 全量替换为 `showToast()`
3. **Playwright E2E测试框架**
   - 安装：`playwright`, `@playwright/test`
   - 第一个测试：单图审查完整流程（上传→审查→查看结果）
   - 第二个测试：批量审查流程
   - 第三个测试：登录/登出流程（P112联动）
4. **验收标准**
   - `innerHTML+=` 残留 = 0
   - `alert()` 残留 = 0
   - ≥3 个 E2E 测试通过

---

### P123 — 前端架构重构（Vite + TypeScript + 组件化） 🔴

**复盘结论：** 10个JS文件全局函数互相调用，104个`var`，72个内联`onclick`，无构建工具，无模块化，无类型检查。任何新增功能都在恶化同一堆代码。

**具体动作：**
1. **Phase 1：Vite + TypeScript 接入（2周）**
   - 引入 `vite` + `@vitejs/plugin-react`（或纯TS）
   - 将10个JS文件逐步迁移为TS模块
   - 全局变量→`import/export`
   - 内联`onclick`→事件监听
2. **Phase 2：组件化（2周）**
   - 抽象组件：`ReviewTable`（违规列表）/ `ReviewItem`（单条违规）/ `DrawingCanvas`（图纸渲染）/ `BatchQueue`（批量队列）/ `Skeleton`（骨架屏）/ `Modal`（弹窗）/ `Toast`（提示）
   - 每个组件独立测试
3. **Phase 3：路由 + 状态管理（1周）**
   - DOM显示/隐藏→前端路由
   - 全局状态→简单的状态管理（不用Redux，用Context API或类似轻量方案）
4. **验收标准**
   - Vite构建产物 < 原CDN加载大小
   - 全局`var` = 0，内联`onclick` = 0
   - 组件化覆盖率 ≥ 80%
   - E2E测试全部通过

---

## P112~P118 原有开发方案（优先级已调整，见上方）

> 以下 P112~P118 为 2026-08-15 规划，复盘后优先级已下调。P111（YOLO训练）已证伪终止。P112/P113/P116 可启动但需依赖P119工作流。P114 被 P122/P123 覆盖。P115/P117/P118 保持原优先级。

### P111 — YOLOv8m 训练 ❌ **已终止**

> **终止原因（2026-08-17 复盘）：** P111 v2全量18类训练 mAP50=0.001，伪标签路线已证伪。
> **替代方案：** 消防设备类改走LAYER_RULES规则引擎 + 几何特征规则（尺寸/位置/文字标签），不依赖视觉模型。

### P111 — YOLOv8m 建筑图元检测模型训练闭环（最高价值）
**价值**：扫线法已解决 room/corridor/doorway，但消防类低语义实体仍弱：`exit_sign` / `sprinkler_system` / `fire_alarm` / `fire_window` / `insulation` / `evacuation_lighting` / `refuge_floor` 等。YOLO 是补这些“图形符号/标注”类别的性价比最高路径。

**当前资产**：
- `data/p106_yolo_dataset/data.yaml`：18 类，训练/验证图像 238，标签文件 179
- `src/baa_engine/yolo_integrator.py`：已有 `load_model → render_and_predict → detections_to_entities` 全链路
- 候选模型路径优先级：`baa_yolov8m_v7_v5` → `baa_yolov8n_v4` → `v3` → `v2-3`

**具体动作**：
1. **数据质量清洗**
   - 删除/重标统计中 `fail=9`、`skip=183`、`timeout=60` 的异常标注
   - 保留真实建筑平面图，剔除图例/目录/计算表/系统拓扑图
   - 统一类别口径：YOLO 类别必须与 `YOLO_CLASSES` 索引一致
2. **扩充 8 类缺失样本**
   - 每类至少 50 张有效图；低语义类别优先（exit_sign、sprinkler、fire_alarm 等）
   - 使用 PDF→DXF→渲染伪标签管线自动扩样，人工抽 5% 抽检
3. **训练与评估**
   - `yolov8m` 为主，训练目标：mAP50 ≥ 0.45；10 类主力类别 mAP50 ≥ 0.55
   - 指标写入 `data/models/<model_name>/metrics.json`
4. **接入主流程**
   - `YOLODetectionIntegrator.load_model()` 优先加载 v8m 新模型
   - `detections_to_entities()` 与扫线法实体做 NMS + IoU 合并，避免重复
5. **验收标准**
   - 10 类主力实体误报/漏检率较 v7_v5 下降 ≥20%
   - 全量测试回归 2115+ 不新增失败

### P112 — 用户认证、团队空间与协作前端落地
**价值**：当前 P43 多用户协作后端已存在，但前端不可达，导致多租户、多人批注、审批流没有产品形态。没有用户系统，产品无法从“本地演示”进入“项目协作”。

**具体动作**：
1. **前端认证入口**
   - 顶部导航增加「登录/注册/退出」入口
   - 统一 token 存储与 `Authorization: Bearer <token>` 请求头
2. **角色与空间切换**
   - 登录后显示团队/项目选择器
   - 未登录态默认展示公开能力与注册引导
3. **协作页面闭环**
   - 团队列表、项目列表、成员邀请、审批记录、违规评论
   - 审查记录与协作文档关联到 `team_id/project_id`
4. **权限保护**
   - 管理 API、密钥、审查导出、批量上传端点鉴权
5. **验收标准**
   - 可完成：注册 → 登录 → 创建团队/项目 → 上传图纸 → 查看审查结果 → 评论审批
   - 至少新增 8 个 E2E 测试用例

### P113 — 真实用户试用与黄金标准扩库
**价值**：模型和规则再优化 10 个点，也不如 3 个真实项目暴露真实需求。当前所有图纸仍是测试集，缺少审图机构/设计院反馈。

**具体动作**：
1. **选取 3~5 个真实项目包**
   - 住宅/办公/数据中心/医院/工业各 1 类，避免建筑类型偏差
2. **建立黄金标准 v2**
   - 每个项目列出真实违规、误报、漏检、不可判定项
   - 与 P97 方式一致：黄金标准变更必须有审查证据
3. **试用反馈闭环**
   - 用户提交误报/漏检 → `feedback_engine` 入库 → 原子函数/模型调整
4. **验收标准**
   - 至少形成 3 份真实项目审计报告
   - 黄金标准扩充 ≥100 条，且 0 个无依据新增 FAIL

### P114 — 前端架构重构：组件化 + E2E 测试
**价值**：当前前端存在 `baa-ext.js` 遗留债、全局函数泛滥、SSE 加载顺序依赖 `onclick` 硬编码绕过。随着用户系统、协作和批量审查增长，前端会快速失控。

**具体动作**：
1. **统一入口**
   - 合并/梳理 `baa-core.js`、`baa-review.js`、`baa-ext.js` 的职责边界
2. **组件化改造**
   - 表格、违规卡、进度条、骨架屏、Modal、上传组件抽象为复用组件
   - 禁止新增 `alert()`，统一走 `showToast`
3. **SSE 批处理改造**
   - 将 `onclick="runBatchReviewSSE()"` 改为注册事件监听
   - 增加断线重试、文件级错误恢复、完成下载
4. **E2E 测试**
   - 用 Playwright/Cypress 覆盖：单图审查、批量审查、报告导出、模型参数导出、协作登录
5. **验收标准**
   - 新增 ≥10 个 E2E 用例
   - 移除/隔离 `baa-ext.js` 中非兼容逻辑

### P115 — 云端部署流水线与生产配置
**价值**：当前 Docker 就绪，但缺真实生产部署、日志、监控、回滚。没有部署闭环，产品无法交付给外部用户。

**具体动作**：
1. **Docker Compose 生产配置**
   - gunicorn workers 按 CPU 自动计算
   - SQLite → PostgreSQL（生产默认）
   - 对象存储/文件挂载策略明确
2. **CI/CD**
   - GitHub Actions 增加 build image、lint、test、E2E、部署 dry-run
   - 标签发布自动构建 Docker image
3. **运维可观测**
   - 结构化日志、健康检查、审查耗时/错误率指标
   - 关键接口错误告警（上传失败、审查超时、GPU/内存不足）
4. **验收标准**
   - 一键 `docker compose up -d` 可启动生产配置
   - CI 每次 push 自动跑 lint/test/E2E

### P116 — 报告产品化与导出升级
**价值**：审查结果需要可直接给甲方/审图机构使用。当前报告已经存在，但需要更像正式产品输出。

**具体动作**：
1. **报告模板统一**
   - 封面、项目信息、图纸信息、总体评分、违规 TOP-N、条款索引、结论页
2. **多格式导出**
   - PDF、HTML、JSON、Excel 违规清单
3. **审图机构视角增强**
   - 条款依据、整改建议、风险等级、复查状态
4. **验收标准**
   - 报告页面无中文乱码、无空白章节
   - 导出接口新增完整测试

### P117 — 模型参数、规范版本与知识库开放
**价值**：当前已有模型参数导出和 OpenAPI/SDK，下一阶段应把它们变成可运营的知识资产，支撑大模型微调或第三方集成。

**具体动作**：
1. **知识库结构化**
   - 原子函数、图层规则、CD 项、案例库统一 schema
2. **版本化发布**
   - 规范版本、模型版本、数据集版本联动
3. **第三方集成包**
   - SDK 增加 batch review、callback/webhook、auth 示例
4. **验收标准**
   - 能一键导出审查知识库包
   - SDK 示例可在 10 分钟内跑通

### P118 — 商业模式与产品定位验证（产品门槛）
**价值**：技术闭环不等于商业闭环。当前必须明确目标客户、付费模式和最小可售卖版本。

**具体动作**：
1. **定义 MVP 产品边界**
   - 本地部署版 / SaaS版 / 审图机构插件版
2. **价格与套餐**
   - 按图纸数、团队数、模型能力、报告导出次数分层
3. **客户验证**
   - 至少访谈 5 个设计院/审图机构/消防咨询客户
4. **验收标准**
   - 输出 1 页产品定位文档、1 页定价表、3 个客户试用计划

---

---

## v2.5.40-stable 发布记录

- **v2.5.45-stable**（`0b7e5f0`，2026-08-15）：P110 工程重构收尾 — 骨架屏API + SSE流式批量审查 + 并发限制
- **v2.5.44-stable**（`196989d`，2026-08-15）：P109 扫线法房间属性自动推断 — 14类规则几何特征+文本关键词
- **v2.5.43-stable**（`9373724`，2026-08-14）：P106 YOLO 伪标签管线 — 179 张标注 / 22051 标签 / 10 类
- **v2.5.42-stable**（`fd92e2b`，2026-08-14）：P107 扫线法→EVAC 闭环 + P108 门洞→DIM 自动触发
- **v2.5.41-stable**（`d2319ad`，2026-08-14）：P107 扫线法 doorway → EVAC 连通性闭环
- **v2.5.40-stable**（`181db3c`）
- **发布日**：2026-08-14
- **关键交付**：P105 走廊面分类 + 门洞间隙检测
- **测试**：2078/2079（1 known-fail: test_multi_page_pdf PDF 文件缺失）
- **总测试函数**：591（21 测试文件）
- **服务状态**：gunicorn 2 workers，`0.0.0.0:8000`，零错误
- **日志清理**：旧实例 traceback 噪声归档至 `data/logs/archive/`，当前日志干净
